import * as React from "react";
import { IOnePartProps } from "../webparts/onePart/components/IOnePartProps";

export default class CandidateForm extends React.Component<IOnePartProps, {}> {
    public render(): React.ReactElement<IOnePartProps> {
        return (
          <p>IOnePartProps</p>
        );
      }
}